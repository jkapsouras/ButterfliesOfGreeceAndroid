<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/field_dark"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <TextView
        android:id="@+id/tv_images_per_page"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/images"
        android:textSize="12sp"
        android:gravity="center"
        android:textColor="@color/field"
        android:layout_margin="@dimen/medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <Spinner
        android:id="@+id/spinner_images_per_page"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:spinnerMode="dialog"
        android:backgroundTint="@color/field"
        android:padding="0dp"
        app:layout_constraintTop_toTopOf="@+id/tv_images_per_page"
        app:layout_constraintStart_toEndOf="@+id/tv_images_per_page"
        />

    <TextView
        android:id="@+id/tv_print_to_pdf_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:text="@string/photos"
        android:textSize="12sp"
        android:gravity="center"
        android:textColor="@color/field"
        android:layout_margin="@dimen/medium"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <ImageButton
        android:id="@+id/iv_delete_images"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_margin="@dimen/medium"
        android:src="@drawable/delete_icon"
        android:tint="@color/field"
        android:background="@android:color/transparent"
        android:scaleType="fitCenter"
        android:padding="4dp"
        app:layout_constraintTop_toTopOf="@+id/iv_print_images"
        app:layout_constraintBottom_toBottomOf="@+id/iv_print_images"
        app:layout_constraintEnd_toStartOf="@+id/iv_print_images"
        />

    <ImageButton
        android:id="@+id/iv_print_images"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_margin="@dimen/medium"
        android:src="@drawable/print_icon"
        android:tint="@color/field"
        android:background="@android:color/transparent"
        android:scaleType="fitCenter"
        android:padding="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        />

</androidx.constraintlayout.widget.ConstraintLayout>